# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in git_cassette.gemspec

gem 'git', '~> 1.18'  # For git operations
gem 'benchmark-ips', '~> 2.10'  # For benchmarking
gem 'fileutils'  # For file operations

group :development do
  gem 'cucumber', '~> 9.1'
  gem 'aruba', '~> 2.1'
  gem 'rspec', '~> 3.12'
end
